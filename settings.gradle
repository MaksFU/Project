dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        maven { url 'https://jitpack.io' }//Для графиков (MPAndroidChart)
    }
}
rootProject.name = "TestDB"
include ':app'
